{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "${workspaceFolder}/GenerateBT_Recursive/main.cpp",
                "-o",
                "GenerateBT_Recursive/main"
            ],
            "problemMatcher":[]
        },
        {
            "label": "run",
            "type":  "shell",
            "command": "${workspaceFolder}/GenerateBT_Recursive/main",
            "dependsOn":[
                "build"
            ],
            "problemMatcher":[]
        }
    ]
}