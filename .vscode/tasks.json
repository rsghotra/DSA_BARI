{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "${workspaceFolder}/DeckOfCards/DeckOfCards.cpp",
                "${workspaceFolder}/DeckOfCards/main.cpp",
                "-o",
                "DeckOfCards/main"
            ],
            "problemMatcher":[]
        },
        {
            "label": "run",
            "type":  "shell",
            "command": "${workspaceFolder}/DeckOfCards/main",
            "dependsOn":[
                "build"
            ],
            "problemMatcher":[]
        }
    ]
}